# 前端环境变量模板:复制为 frontend/.env 后按需修改(frontend/.env 已被 gitignore,
# 不会被提交 —— 避免将来向同一文件添加真实配置时误入库)。
# 不创建该文件时,代码使用与下面相同的默认值,开发环境开箱即用。

# 后端 API 的基础路径。vite 开发服务器把 /api 代理到 http://localhost:9090
# (见 vite.config.ts),生产环境按实际部署路径修改。
VITE_API_BASE_URL=/api/ragent

# 应用名称(可选,当前代码未消费此变量)。
VITE_APP_NAME=Ragent 智能问答
